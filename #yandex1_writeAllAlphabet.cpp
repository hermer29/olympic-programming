/*
1 [Yandex]
Функция принимает строку, а возвращает пару индексов таких, что подмассив, ограниченный
ими содержит весь алфавит данной строки, при том является минимальным. Под алфавитом под-
разумевается набор символов, каждый их которых хотя бы раз входит в изначальную строку

Это leetcode76 - minimum window substring
*/

void main() {
    
}