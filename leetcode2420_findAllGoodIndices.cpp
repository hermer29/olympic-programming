#include "includes.cpp"

class Solution {
public:
    vector<int> goodIndices(vector<int>& nums, int k) {
        int n = nums.size();
        vector<int> results;

        for(int i = k; i < n - k; i++) {
            
        }
    }
};